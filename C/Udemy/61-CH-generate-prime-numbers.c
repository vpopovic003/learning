
#include <stdio.h>

int main(){
    int primes[100] = {1, 2};

    int i = 3;
    for (i = 3; i <= 100; i++){

        int x = 0;
        for (x = 0; x <= i; x ++){
            if (i % primes[x] = 0)
                
        }
        printf("\n%d\n", primes[x]);
    }

    return 0;
}
